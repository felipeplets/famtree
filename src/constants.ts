// Shared geometry constants for layout and rendering.
export const NODE = 46;         // node width / diameter
export const R = NODE / 2;      // node radius
export const GEN_SPACING = 150; // vertical distance between generations
export const SIB_STEP = 128;    // horizontal step between sibling subtrees
export const SPOUSE_HALF = 64;  // half distance between partners in a couple
export const MARGIN = 70;       // outer margin / frame padding
export const LABEL_WRAP = 16;   // max characters per label line before wrapping
export const LABEL_LINE_H = 13; // vertical distance between wrapped label lines
